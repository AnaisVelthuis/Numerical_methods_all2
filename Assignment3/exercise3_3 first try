function[]=dft(vector)

    n=length(vector);
    w=ones(1,n);
    w(n) = exp(-2*pi*i/n);
   
    if (n ==1) 
        y=vector; 
    else 
        dft(vector(1:n))=[dft(vector(1:2:n))+ w^(0:n/2-1)*dft(vector(2:2:n));
           dft(vector(1:2:n))-w^(0:n/2-1)*dft(vector(2:2:n))]
    end 
y
end 
